import React from 'react'
import ReactDOM from 'react-dom/client'
import AionOS from './AionOS.jsx'
import './style.css'

ReactDOM.createRoot(document.getElementById('app')).render(
  <React.StrictMode>
    <AionOS />
  </React.StrictMode>,
)
